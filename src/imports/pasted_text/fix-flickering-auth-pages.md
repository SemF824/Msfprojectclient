Tu es un expert React/TypeScript/Vite/Vercel. Corrige ces 4 problèmes dans l'ordre exact. Ne modifie pas le design (couleurs #0a0f1e/#d4af37, animations existantes, layout général). --- ## PROBLÈME 1 : Clignotements (flickering) sur Home et Contact sur Vercel Les animations whileInView de Framer Motion causent des repaints GPU excessifs sur Vercel Edge. Applique ces corrections : ### 1A — Dans TOUS les composants qui utilisent motion.div avec whileInView :Statistics.tsx, About.tsx, Vision.tsx, Projects.tsx, Expertise.tsx, FeaturedProperties.tsx, PropertyFilter.tsx, Contact.tsxAssure-toi que CHAQUE motion.div avec whileInView a : - viewport={{ once: true, margin: "-50px" }} - la classe Tailwind `will-change-transform` sur l'élément animé - transition={{ duration: 0.4 }} au lieu de durées plus longues ### 1B — Dans FeaturedProperties.tsx, remplace les motion.div de la grille par : ```tsx <motion.div key={property.id} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, margin: "-50px" }} transition={{ delay: index * 0.1, duration: 0.4 }} className="group relative bg-white/5 backdrop-blur-xl rounded-2xl overflow-hidden border border-white/10 hover:border-[#d4af37]/50 transition-all duration-500 hover:shadow-2xl hover:shadow-[#d4af37]/20 will-change-transform" > ``` ### 1C — Dans Statistics.tsx, ajoute layout="position" et will-change-transform : ```tsx <motion.div key={index} layout="position" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1, duration: 0.4 }} className="relative group h-full will-change-transform" > ``` ### 1D — Dans Home.tsx, wraps chaque section dans une div avec hauteur minimum pour éviter les layout shifts : ```tsx export default function Home() { return ( <div className="min-h-screen"> <Hero /> <div className="min-h-[200px]"><Statistics /></div> <div className="min-h-[400px]"><About /></div> <div className="min-h-[400px]"><Vision /></div> <div className="min-h-[600px]"><Projects /></div> <div className="min-h-[400px]"><Expertise /></div> <PropertyFilter /> <FeaturedProperties /> </div> ); } ``` ### 1E — Dans FeaturedProperties.tsx, ajoute loading="lazy" et les dimensions sur toutes les balises img : ```tsx <img src={property.image} alt={property.title} loading="lazy" width="800" height="600" className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" /> ``` --- ## PROBLÈME 2 : Pages noires / introuvables après connexion ou inscription Après signUp ou signIn Supabase, l'app redirige vers des pages noires car l'état d'authentification n'est pas encore propagé quand navigate() est appelé. ### 2A — Dans src/app/pages/Login.tsx, remplace le handleSubmit par : ```tsx const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setIsLoading(true); setError(null);try { const { error: signInError } = await signIn(formData.email, formData.password); if (signInError) {setError(signInError.message || "Identifiants incorrects"); setIsLoading(false); return; } // La redirection sera gérée par le useEffect navigate("/dashboard"); } catch (err: any) { setError(err.message || "Une erreur est survenue");setIsLoading(false); } }; ``` Ajoute ce useEffect dans Login.tsx pour rediriger automatiquement si déjà connecté : ```tsxuseEffect(() => { if (user) { navigate("/dashboard", { replace: true }); } }, [user, navigate]); ``` ### 2B — Dans src/app/pages/Signup.tsx, remplace le handleSubmit par : ```tsx const handleSubmit = async (e: React.FormEvent) => {e.preventDefault(); if (formData.password !== formData.confirmPassword) { setError("Les mots de passe ne correspondent pas"); return; } setIsLoading(true); setError(""); try { const { data, error: signUpError } = await supabase.auth.signUp({ email: formData.email, password: formData.password, options: { data: { full_name: `${formData.firstName} ${formData.lastName}`, first_name: formData.firstName, last_name: formData.lastName,phone: formData.phone, city: formData.city, } } }); if (signUpError) { setError(signUpError.message);setIsLoading(false); return; } // Rediriger vers l'accueil (pas dashboard) car email pas encore confirmé // Si auto-confirm est activé dans Supabase, aller au dashboard if (data.session) { navigate("/dashboard", { replace: true }); } else { // Email de confirmation envoyé - aller à l'accueil avec message navigate("/", { replace: true }); } } catch (err: any) {setError("Erreur lors de l'inscription. Veuillez réessayer."); setIsLoading(false); } }; ``` ### 2C — Dans src/app/pages/Dashboard.tsx, ajoute une redirection vers /connexion si non connecté : ```tsx // Juste après le guard de chargement isLoading, ajoute : if (!isLoading && !authUser) { return <Navigate to="/connexion" replace />; } ```Importe Navigate depuis react-router. ### 2D — Dans src/app/AdminApp.tsx, dans ProtectedAdminRoute, s'assurer qu'on retourne Navigate et non window.location : Le composant ProtectedAdminRoute doit déjà utiliser <Navigate to="/admin" replace /> — vérifie que c'est bien le cas et non window.location.href. ### 2E — Dans src/app/App.tsx, s'assurer que la détection admin fonctionne sur Vercel. Sur Vercel le hostname peut être différent. Remplace la détection par : ```tsxuseEffect(() => { const hostname = window.location.hostname; const pathname = window.location.pathname; const searchParams = new URLSearchParams(window.location.search); if ( hostname.startsWith('admin.') ||pathname.startsWith('/admin') || searchParams.get('admin') === 'true' ) { setAppType('admin'); } else {setAppType('client'); } }, []); ``` --- ## PROBLÈME 3 : Pages noires ou "Not Found" quand une route est inconnue Quand l'utilisateur navigue vers une URL inconnue, il voit une page noire. Il faut un fallback qui ramène à l'accueil. ### 3A — Dans src/app/pages/NotFound.tsx, remplace le contenu par : ```tsx import { useEffect } from "react"; import { Link, useNavigate } from "react-router"; import { Home, ArrowLeft } from "lucide-react"; import { motion } from "motion/react"; export default function NotFound() { const navigate = useNavigate(); // Redirection automatique vers l'accueil après 5 secondes useEffect(() => { const timer = setTimeout(() => { navigate("/", { replace: true }); }, 5000);return () => clearTimeout(timer); }, [navigate]); return ( <div className="min-h-screen bg-gradient-to-br from-slate-50 via-gray-50 to-blue-50 flex items-center justify-center px-6"> <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="max-w-2xl w-full text-center" > <div className="mb-8"> <h1 className="text-9xl md:text-[12rem] font-bold text-transparent bg-clip-text bg-gradient-to-r from-[#d4af37] to-[#f4e3b2]"> 404 </h1> </div><div className="bg-white rounded-2xl border border-gray-200 shadow-lg p-8 md:p-12"> <div className="w-20 h-20 bg-gradient-to-br from-[#d4af37] to-[#f4e3b2] rounded-full flex items-center justify-center mx-auto mb-6"> <Home className="w-10 h-10 text-[#0a0f1e]" /> </div> <h2 className="text-3xl text-[#0a0f1e] font-bold mb-4"> Page Introuvable </h2> <p className="text-gray-600 text-lg mb-4"> Cette page n'existe pas. Vous allez être redirigé automatiquement vers l'accueil dans 5 secondes. </p> <div className="flex flex-col sm:flex-row gap-4 justify-center"><button onClick={() => navigate(-1)} className="flex items-center justify-center gap-2 px-6 py-3 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-colors" > <ArrowLeft className="w-4 h-4" /> Retour </button><Link to="/" className="flex items-center justify-center gap-2 px-6 py-3 bg-gradient-to-r from-[#d4af37] to-[#f4e3b2] text-[#0a0f1e] rounded-xl hover:shadow-2xl hover:shadow-[#d4af37]/40 transition-all font-medium" > <Home className="w-4 h-4" /> Retour à l'Accueil </Link> </div> </div> </motion.div> </div> ); } ``` ### 3B — Dans src/app/pages/admin/AdminNotFound.tsx, applique la même logique avec redirection vers /dashboard après 5 secondes.### 3C — Dans src/app/ClientApp.tsx, s'assurer qu'il y a une route catch-all : ```tsx { path: "*", Component: NotFound }``` Cette route doit exister DANS le layout ET en dehors pour couvrir tous les cas. --- ## PROBLÈME 4 : Google Maps — IP/domaine Vercel non autorisé L'erreur "This IP, site or mobile application is not authorized to use this API key" vient du fait que la clé Google Maps n'est pas configurée pour accepter le domaine Vercel. ### 4A — Dans Contact.tsx et PropertyDetails.tsx, remplace le composant GoogleMap/PropertyMap par une version avec gestion d'erreur robuste :```tsx const GoogleMap = () => { const apiKey = import.meta.env.VITE_GOOGLE_MAPS_API_KEY; // Si pas de clé API, afficher un placeholder élégant if (!apiKey) { return ( <div className="h-80 bg-gradient-to-br from-gray-100 to-gray-200 rounded-xl flex items-center justify-center border border-gray-200"> <div className="text-center p-6"><MapPin className="w-16 h-16 text-[#d4af37] mx-auto mb-4" /> <p className="text-[#0a0f1e] font-semibold mb-2">Carte Interactive</p> <p className="text-sm text-gray-600 mb-1">1 place Antonetti, 7ème étage</p> <p className="text-sm text-gray-600 mb-4">Centre-ville, Pointe-Noire, Congo</p> href="https://maps.google.com/?q=Pointe-Noire+Congo" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-4 py-2 bg-[#d4af37] text-[#0a0f1e] rounded-lg text-sm font-medium hover:shadow-lg transition-all" > <MapPin className="w-4 h-4" /> Ouvrir dans Google Maps </a> </div> </div> ); } return ( <div className="h-80 rounded-xl overflow-hidden border border-gray-200"> <iframe width="100%" height="100%" frameBorder="0" style={{ border: 0 }}referrerPolicy="no-referrer-when-downgrade" src={`https://www.google.com/maps/embed/v1/place?key=${apiKey}&q=Place+Antonetti+Pointe-Noire+Congo&zoom=15&language=fr`} allowFullScreentitle="Localisation MSF Congo" onError={() => console.warn("Google Maps failed to load")} /> </div> ); }; ``` ### 4B — Ajoute ces instructions dans DEPLOIEMENT.md pour que le développeur configure sa clé : ```markdown ## ⚠️ Configuration Google Maps sur Vercel Pour que la carte fonctionne sur Vercel, tu dois : 1. Aller sur https://console.cloud.google.com 2. APIs & Services > Credentials > ta clé Maps 3. Sous "Application restrictions", choisir "HTTP referrers (websites)" 4. Ajouter ces domaines autorisés : - https://msfprojectclient.vercel.app/* - https://*.vercel.app/* - http://localhost:5173/* - https://msfcongo.com/* (si domaine custom) 5. Sous "API restrictions", choisir "Maps Embed API" 6. Sauvegarder et attendre 5 minutes 7. Dans Vercel > Settings > Environment Variables, ajouter : VITE_GOOGLE_MAPS_API_KEY = ta_clé_ici 8. Redéployer ``` --- ## RÉSUMÉ DES FICHIERS À MODIFIER 1. src/app/pages/Home.tsx 2. src/app/components/Statistics.tsx 3. src/app/components/About.tsx 4. src/app/components/Vision.tsx 5. src/app/components/Projects.tsx 6. src/app/components/Expertise.tsx 7. src/app/components/FeaturedProperties.tsx 8. src/app/pages/Login.tsx 9. src/app/pages/Signup.tsx 10. src/app/pages/Dashboard.tsx 11. src/app/pages/NotFound.tsx 12. src/app/pages/admin/AdminNotFound.tsx 13. src/app/pages/Contact.tsx 14. src/app/pages/PropertyDetails.tsx 15. src/app/App.tsx 16. src/app/ClientApp.tsx 17. DEPLOIEMENT.md Ne modifie pas : les couleurs, le thème doré/marine, les composants UI Radix, ni l'architecture client/admin.
